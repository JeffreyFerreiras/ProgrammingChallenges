pub struct Solution;

impl Solution {
    pub fn k_closest(_points: Vec<Vec<i32>>, _k: i32) -> Vec<Vec<i32>> {
        todo!("Maintain a max-heap of size k tracking the closest points by squared distance.");
    }
}
