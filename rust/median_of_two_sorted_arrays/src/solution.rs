pub struct Solution;

impl Solution {
    pub fn find_median_sorted_arrays(_nums1: Vec<i32>, _nums2: Vec<i32>) -> f64 {
        todo!("Partition the two arrays to compute the median in logarithmic time.");
    }
}
